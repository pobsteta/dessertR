% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canopee.R
\name{dsr_gabarit_lateral}
\alias{dsr_gabarit_lateral}
\title{Largeur de la trouee de canopee et surplomb le long d'un trace}
\usage{
dsr_gabarit_lateral(
  trace,
  chm,
  largeur = NULL,
  pas = 2,
  demi_largeur = 8,
  pas_travers = 0.5,
  seuil_ouvert = 2,
  liss_travers = 1
)
}
\arguments{
\item{trace}{Un \code{sf}/\code{sfc} \code{LINESTRING} (ou l'element \code{trace} de
\code{\link[=dsr_pathfinder]{dsr_pathfinder()}}).}

\item{chm}{\code{SpatRaster} de hauteur de vegetation (CHM predit depuis l'ortho,
ou MNH lidar). Une seule bande utilisee.}

\item{largeur}{Largeur roulable par station, pour le surplomb : un vecteur
numerique (longueur 1 ou nombre de stations), ou le \code{sf} \code{stations} de
\code{\link[=dsr_measure]{dsr_measure()}} dont la colonne \code{LARGEUR_ROULABLE} est reprise. \code{NULL}
(defaut) : \code{SURPLOMB} et \code{HAUT_SURPLOMB} valent \code{NA}. Le decoupage en
stations etant deterministe, un \code{dsr_measure()} et un
\code{dsr_gabarit_lateral()} appeles sur le meme trace avec le meme \code{pas}
donnent le meme nombre de stations.}

\item{pas}{Espacement des stations le long du trace, en metres. Defaut 2.}

\item{demi_largeur}{Demi-largeur des profils, en metres. Defaut 8. Une trouee
plus large que \code{2 * demi_largeur} est tronquee (colonne \code{TRONQUE}).}

\item{pas_travers}{Pas d'echantillonnage transversal, en metres. Defaut 0.5.}

\item{seuil_ouvert}{Hauteur (m) sous laquelle une cellule compte comme
degagee. Defaut 2 : en deca on est dans l'herbe et le semis, qui ne genent
pas un grumier lateralement.}

\item{liss_travers}{Fenetre de lissage transversal (nombre d'echantillons,
impair). Defaut 1 (aucun lissage) : contrairement au MNT, un CHM porte de
vraies ruptures qu'il ne faut pas arrondir.}
}
\value{
Un \code{sf} \code{POINT} par station, avec \code{chainage}, \code{LARGEUR_DEGAGEE} (m),
\code{DEGAGE_G} et \code{DEGAGE_D} (distance de l'axe au bord de trouee, m, par
cote), \code{TRONQUE} (la trouee sort du profil), et si \code{largeur} est fourni
\code{SURPLOMB} (m d'emprise recouverte, 0 si aucun) et \code{HAUT_SURPLOMB} (hauteur
du plus bas houppier empietant, lue au plus proche voisin pour rester une
valeur de cellule reelle ; \code{Inf} si aucun).
}
\description{
Mesure, station par station, la largeur de la \strong{trouee de canopee} centree
sur l'axe -- la plage continue ou la hauteur de vegetation reste sous
\code{seuil_ouvert} -- et, si la largeur de chaussee est fournie, le \strong{surplomb} :
de combien les houppiers empietent sur l'emprise roulable. C'est le critere
lateral absent de \code{\link[=dsr_trafficability]{dsr_trafficability()}}, et la reponse a la question
operationnelle \emph{ou elaguer}.
}
\details{
\strong{Ce n'est pas une mesure de largeur de chaussee.} La trouee et la chaussee
divergent de facon variable selon le peuplement riverain ; voir l'en-tete du
fichier. \code{LARGEUR_DEGAGEE} ne doit jamais alimenter
\code{\link[=dsr_calibrer_largeur]{dsr_calibrer_largeur()}}.

\strong{Le surplomb est detecte, sa hauteur est surestimee.} Un modele de hauteur
de canopee donne le \strong{sommet} du houppier, pas le dessous de la branche. Une
branche basse d'un arbre de 25 m est vue a 25 m : \code{HAUT_SURPLOMB} est donc un
indicateur \strong{permissif}, qui attrape a coup sur la regeneration et les
rejets de bord de route (le cas dominant) et rate les branches basses des
grands arbres. Seul \code{\link[=dsr_gabarit_libre]{dsr_gabarit_libre()}}, sur le nuage classe, donne le
dessous de branche.

\strong{Attention a la maille reelle.} Un CHM predit par les modeles Open-Canopy
a une maille native de l'ordre de 1,5 m ; sureechantillonne a 0,20 m il
\emph{declare} 0,20 m mais ne porte que l'information de sa maille d'origine. Le
paquet ne peut pas detecter ce cas -- la fonction ne signale que la
resolution declaree. Pour une mesure au decimetre, il n'y a que le MNT.
}
\examples{
\donttest{
chm <- terra::rast(xmin = 0, xmax = 60, ymin = 0, ymax = 60,
  resolution = 1, crs = "EPSG:2154")
terra::values(chm) <- 20
# Un couloir degage de 6 m de large autour de y = 30
xy <- terra::xyFromCell(chm, seq_len(terra::ncell(chm)))
chm[abs(xy[, 2] - 30) <= 3] <- 0
tr <- sf::st_sf(geometry = sf::st_sfc(
  sf::st_linestring(cbind(c(5, 55), c(30, 30))), crs = 2154))
g <- dsr_gabarit_lateral(tr, chm, largeur = 4)
summary(g$LARGEUR_DEGAGEE)
}
}
\seealso{
\code{\link[=dsr_gabarit_libre]{dsr_gabarit_libre()}} pour le gabarit vertical sur le nuage,
\code{\link[=dsr_trafficability]{dsr_trafficability()}} qui consomme \code{SURPLOMB} et \code{HAUT_SURPLOMB}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers_dtm.R
\name{dsr_micro_relief}
\alias{dsr_micro_relief}
\title{Canaux de micro-relief openness et SVF d'un MNT}
\usage{
dsr_micro_relief(
  mnt,
  radius_m = 10,
  radius_min_m = NULL,
  num_directions = 16L,
  canaux = c("svf", "openness_pos", "openness_neg")
)
}
\arguments{
\item{mnt}{Le MNT, \code{SpatRaster} mono-couche (cellules supposees carrees ; la
premiere couche est prise si plusieurs) ou chemin de GeoTIFF.}

\item{radius_m}{Rayon de recherche d'horizon maximal, en \strong{metres}. Defaut 10.}

\item{radius_min_m}{Rayon minimal en metres (reduction du bruit). Defaut
\code{NULL} -> un pixel.}

\item{num_directions}{Nombre de directions azimutales balayees. Defaut 16.}

\item{canaux}{Canaux a renvoyer, parmi \code{"svf"}, \code{"openness_pos"},
\code{"openness_neg"}. Defaut : les trois.}
}
\value{
Un \code{SpatRaster} aligne sur \code{mnt}, une couche par canal demande
(\code{svf} en 0..1, \code{openness_pos}/\code{openness_neg} en degres), \code{NA} la ou le MNT
est \code{NA}. Les noms coincident avec le vocabulaire de \code{\link[=dsr_canaux_externes]{dsr_canaux_externes()}}.
}
\description{
Derive le \strong{sky-view factor} et l'\strong{openness} (positive et negative) d'un
modele de terrain -- les canaux morphometriques qui portent la signature d'une
desserte (depression de plateforme, talus amont et aval, fosses lateraux) bien
mieux que le MNT brut (BRIEF section 3.2). Enveloppe SIG du noyau Rust
\code{rvt_svf_opns} (portage valide de la Relief Visualization Toolbox, Apache 2.0,
vendorise depuis le paquet \code{foretaccess}) : le balayage d'horizon vit dans le
crate, \code{terra} ne fait que recoller les canaux sur la grille du MNT.
}
\details{
L'\strong{openness negative} est l'openness du MNT \strong{inverse} (elle allume les
talus amont et les fosses), calculee ici en passant le noyau sur \code{-z}.

Le rayon de recherche est donne en \strong{metres} et converti en pixels selon la
resolution du MNT. Alimenter un MNT a \strong{1 m ou plus grossier} (voir
\code{\link[=dsr_grille_reference]{dsr_grille_reference()}}) : a 50 cm le balayage coute ~4 fois plus et le petit
rayon ne capte que du bruit d'interpolation. Pour la pile multi-echelle du
BRIEF, appeler la fonction a plusieurs rayons (2 / 5 / 10 m).
}
\examples{
mnt <- terra::rast(
  nrows = 30, ncols = 30, xmin = 0, xmax = 30, ymin = 0, ymax = 30,
  crs = "EPSG:2154"
)
terra::values(mnt) <- 100 # terrain plat -> svf = 1, openness = 90
mr <- dsr_micro_relief(mnt, radius_m = 5)
names(mr)
}
\seealso{
\code{\link[=dsr_grille_reference]{dsr_grille_reference()}}, \code{\link[=dsr_canaux_externes]{dsr_canaux_externes()}}.
}

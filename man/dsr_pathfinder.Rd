% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pathfinder.R
\name{dsr_pathfinder}
\alias{dsr_pathfinder}
\title{Trace de moindre cout anisotrope entre deux points}
\usage{
dsr_pathfinder(
  sigma_geo,
  depart,
  arrivee,
  theta = NULL,
  poids = NULL,
  k = 16L,
  lambda = 4,
  mu = 2,
  sigma_min = 0.05,
  cout_cumule = FALSE
)
}
\arguments{
\item{sigma_geo}{Conductivite geomorphologique, \code{SpatRaster} mono-couche
(sortie de \code{\link[=dsr_conductivite]{dsr_conductivite()}}) ; \code{NA} = infranchissable.}

\item{depart, arrivee}{Extremites : un \code{POINT} \code{sf}/\code{sfc}, un couple
numerique \code{c(x, y)}, ou un indice de cellule entier.}

\item{theta}{Orientation locale des lineaires en degres (\code{vesselness} /
\code{theta} de \code{\link[=dsr_layers_dtm]{dsr_layers_dtm()}}), aligne sur \code{sigma_geo} ; \code{NULL} -> isotrope.}

\item{poids}{Force de l'anisotropie par cellule (p. ex. \code{vesselness}, 0..1),
aligne sur \code{sigma_geo} ; \code{NULL} -> 0 (isotrope).}

\item{k}{Nombre de caps discrets de l'etat d'orientation. Defaut 16.}

\item{lambda}{Poids de l'anisotropie (penalite de traverse). Defaut 4.}

\item{mu}{Poids de la courbure (penalite de changement de cap). Defaut 2.}

\item{sigma_min}{Plancher de conductivite (evite les resistances infinies).
Defaut 0.05.}

\item{cout_cumule}{Renvoyer aussi le champ de cout minimal a la source (utile
pour l'incertitude laterale). Defaut \code{FALSE}.}
}
\value{
Une liste : \code{trace} (un \code{sf} \code{LINESTRING}, du depart a l'arrivee, avec
la colonne \code{cout}), \code{cout} (le cout total) et \code{cout_cumule} (un \code{SpatRaster}
ou \code{NULL}).
}
\description{
Recherche le chemin optimal sur la conductivite geomorphologique \code{sigma_geo},
avec un \strong{cout anisotrope} (les deplacements en travers de l'orientation
locale \code{theta} sont penalises : une route a une direction) et une \strong{penalite
de courbure} (BRIEF section 3.5). Le voisinage 16 supprime le biais de
metrication du Dijkstra 8-connexe (traces en escalier). Le pathfinder tourne
sur \code{sigma_geo} (robuste a la vegetation) ; l'etat de la desserte se lit
ensuite le long du trace (\code{\link[=dsr_etat]{dsr_etat()}}).
}
\examples{
\donttest{
sg <- terra::rast(nrows = 40, ncols = 40, xmin = 0, xmax = 40, ymin = 0,
  ymax = 40, crs = "EPSG:2154")
terra::values(sg) <- 0.1
sg[20, ] <- 0.95 # un couloir conducteur horizontal
tr <- dsr_pathfinder(sg, c(1, 20.5), c(39, 20.5))
tr$cout
}
}
\seealso{
\code{\link[=dsr_conductivite]{dsr_conductivite()}}, \code{\link[=dsr_layers_dtm]{dsr_layers_dtm()}}, \code{\link[=dsr_etat]{dsr_etat()}}.
}

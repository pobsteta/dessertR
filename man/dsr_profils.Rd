% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measure.R
\name{dsr_profils}
\alias{dsr_profils}
\title{Profils transversaux le long d'un trace}
\usage{
dsr_profils(
  trace,
  mnt,
  pas = 2,
  demi_largeur = 8,
  pas_travers = 0.5,
  methode = c("bilinear", "simple")
)
}
\arguments{
\item{trace}{Un \code{sf}/\code{sfc} \code{LINESTRING} (ou l'element \code{trace} de
\code{\link[=dsr_pathfinder]{dsr_pathfinder()}}).}

\item{mnt}{Le MNT (\code{SpatRaster}), de preference a 50 cm.}

\item{pas}{Espacement des profils le long du trace, en metres. Defaut 2.}

\item{demi_largeur}{Demi-largeur des profils, en metres. Defaut 8.}

\item{pas_travers}{Pas d'echantillonnage transversal, en metres. Defaut 0.5.}

\item{methode}{Interpolation de l'extraction : \code{"bilinear"} (defaut, pour un
MNT continu) ou \code{"simple"} (plus proche voisin, pour une grille a trous
comme la hauteur de sursol).}
}
\value{
Une liste : \code{stations} (\code{sf} \code{POINT} des centres, avec \code{chainage}),
\code{offsets} (positions transversales, m), \code{z} (matrice \verb{stations x offsets}
des altitudes), \code{normales} (matrice \verb{stations x 2}, vecteur transversal
unitaire par station).
}
\description{
Preleve, tous les \code{pas} metres le long d'un trace, un profil d'altitude
\strong{perpendiculaire} au trace, echantillonne sur le MNT (BRIEF section 3.6).
C'est la matiere premiere de \code{\link[=dsr_measure]{dsr_measure()}}.
}
\seealso{
\code{\link[=dsr_measure]{dsr_measure()}}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/learn.R
\name{dsr_apprendre_conductivite}
\alias{dsr_apprendre_conductivite}
\title{Ajuster une conductivite apprise}
\usage{
dsr_apprendre_conductivite(
  echantillon,
  methode = c("glm", "ranger"),
  k = 5,
  ...
)
}
\arguments{
\item{echantillon}{\code{data.frame} issu de \code{\link[=dsr_echantillon]{dsr_echantillon()}} : colonne \code{y}
(0/1) et une colonne par canal.}

\item{methode}{\code{"glm"} (regression logistique, defaut) ou \code{"ranger"} (foret
aleatoire probabiliste).}

\item{k}{Nombre de plis de la validation croisee stratifiee. \code{NULL} ou \verb{< 2}
pour la sauter. Defaut 5.}

\item{...}{Arguments supplementaires transmis a \code{ranger::ranger()}.}
}
\value{
Un objet de classe \code{dsr_modele_conductivite} : liste \code{fit},
\code{methode}, \code{canaux}, \code{auc_vc} (validation croisee), \code{auc_app}
(apprentissage), \code{n}, \code{prevalence}, \code{k}.
}
\description{
Ajuste sur un echantillon (\code{\link[=dsr_echantillon]{dsr_echantillon()}}) le modele qui remplace la
combinaison parametrique de \code{\link[=dsr_conductivite]{dsr_conductivite()}} quand \code{method = "model"}
(BRIEF sections 3.4 et 5, lot 8).
}
\details{
Deux moteurs. \code{"glm"} (defaut) est une regression logistique : sans
dependance, ses coefficients se lisent, et sur une dizaine de canaux deja
concus pour la tache elle est difficile a battre avec quelques milliers de
cellules. \code{"ranger"} (paquet en Suggests) est une foret aleatoire en mode
probabiliste, utile quand le jeu grossit et que les interactions comptent.

\strong{L'AUC rapportee est celle de la validation croisee stratifiee} a \code{k} plis,
pas celle de l'apprentissage : c'est la seule qui dise quelque chose de la
generalisation. L'ecart entre \code{auc_vc} et \code{auc_app} mesure le surapprentissage.
Un modele ajuste sur un massif n'est pas presume valide sur un autre — le
revalider avant de l'y appliquer (BRIEF section 4).
}
\seealso{
\code{\link[=dsr_echantillon]{dsr_echantillon()}}, \code{\link[=predict.dsr_modele_conductivite]{predict.dsr_modele_conductivite()}},
\code{\link[=dsr_conductivite]{dsr_conductivite()}}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conductivity.R
\name{dsr_conductivite}
\alias{dsr_conductivite}
\title{Conductivite geomorphologique \code{sigma_geo}}
\usage{
dsr_conductivite(
  couches,
  specs = dsr_specs_geomorpho(),
  method = c("param", "model"),
  sigma_min = 0.05,
  confiance = NULL
)
}
\arguments{
\item{couches}{Le \code{SpatRaster} multi-bandes de \code{\link[=dsr_layers_dtm]{dsr_layers_dtm()}} (ou tout
sous-ensemble aligne).}

\item{specs}{Regles d'appartenance ; defaut \code{\link[=dsr_specs_geomorpho]{dsr_specs_geomorpho()}}. Les
canaux sans regle sont ignores.}

\item{method}{\code{"param"} (defaut) pour la combinaison parametrique, ou
\code{"model"} (conductivite apprise) — reserve, non encore implemente.}

\item{sigma_min}{Plancher de conductivite. Defaut 0.05.}

\item{confiance}{\code{SpatRaster} de confiance dans \verb{[0, 1]}, aligne sur
\code{couches} ; \code{NULL} pour ne pas ponderer.}
}
\value{
Un \code{SpatRaster} mono-couche \code{sigma_geo}, valeurs dans \verb{[sigma_min, 1]}.
}
\description{
Fusionne les couches du canal geomorphologique en une conductivite \code{sigma_geo}
dans \verb{[sigma_min, 1]} — la probabilite qu'un pixel porte l'\strong{empreinte} d'une
route (BRIEF section 3.4). La combinaison est une \strong{moyenne geometrique
ponderee} des fonctions d'appartenance, plancher a \code{sigma_min} pour eviter
les zeros infranchissables. Les canaux multi-echelles (p. ex. \code{openness_neg_2}
/ \verb{_5} / \verb{_10}) sont regroupes par nom de base et moyennes avant ponderation.
}
\details{
\strong{Ponderation par la confiance.} La ou la densite de points sol s'effondre,
l'openness devient du bruit : passer cette couche de confiance (normalisee
dans \verb{[0, 1]}, typiquement \code{densite_sol} mise a l'echelle) via \code{confiance}
tire \code{sigma_geo} vers l'\strong{incertain} (0.5) plutot que vers une valeur faible
trompeuse.
}
\examples{
\donttest{
mnt <- terra::rast(
  nrows = 60, ncols = 60, xmin = 0, xmax = 60, ymin = 0, ymax = 60,
  crs = "EPSG:2154"
)
terra::values(mnt) <- 100 + terra::rowFromCell(mnt, seq_len(terra::ncell(mnt))) * 0.1
pile <- dsr_layers_dtm(mnt, res = 1)
sg <- dsr_conductivite(pile)
}
}
\seealso{
\code{\link[=dsr_layers_dtm]{dsr_layers_dtm()}}, \code{\link[=dsr_appartenance]{dsr_appartenance()}}, \code{\link[=dsr_specs_geomorpho]{dsr_specs_geomorpho()}}.
}

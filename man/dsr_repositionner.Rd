% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pathfinder.R
\name{dsr_repositionner}
\alias{dsr_repositionner}
\title{Repositionner un reseau de reference sous contrainte}
\usage{
dsr_repositionner(
  reseau,
  sigma_geo,
  theta = NULL,
  poids = NULL,
  deviation_max = 10,
  attraction = 1,
  lambda = 4,
  mu = 2,
  sigma_min = 0.05
)
}
\arguments{
\item{reseau}{Un \code{sf} de \code{LINESTRING} (reseau de reference, p. ex. BD TOPO).}

\item{sigma_geo}{Conductivite geomorphologique (\code{\link[=dsr_conductivite]{dsr_conductivite()}}),
\code{SpatRaster}.}

\item{theta, poids}{Orientation et force d'anisotropie (\code{\link[=dsr_layers_dtm]{dsr_layers_dtm()}}),
alignes sur \code{sigma_geo} ; \code{NULL} -> isotrope.}

\item{deviation_max}{Ecart lateral maximal a l'axe d'origine, en metres.
Defaut 10.}

\item{attraction}{Force du rappel vers l'axe (0 = aucun ; plus grand = plus
colle a l'axe). Defaut 1.}

\item{lambda, mu, sigma_min}{Parametres du pathfinder (\code{\link[=dsr_pathfinder]{dsr_pathfinder()}}).}
}
\value{
Le \code{sf} d'entree, geometries recalees, avec les colonnes
\code{DEPLACEMENT_MAX} et \code{DEPLACEMENT_MOY} (ecart a l'axe d'origine, m) et
\code{RECALE} (logique : \code{FALSE} = repli sur l'origine).
}
\description{
Recale chaque troncon d'un reseau de reference (BD TOPO) sur le MNT lidar via
le pathfinder, \strong{sans jamais s'ecarter de plus de \code{deviation_max} metres de
l'axe d'origine} (couloir dur) et en etant attire vers cet axe (contrainte
douce). La reference fait autorite : la validation a montre qu'un
repositionnement libre sur \code{sigma_geo} accroche des lineaires paralleles
(fosses, traces fossiles, risque n.1 du BRIEF) et degrade la mesure. La
contrainte l'empeche.
}
\details{
La \strong{totalite du reseau de reference est conservee} : si le pathfinder
echoue sur un troncon, on garde sa geometrie d'origine. Le recalage ne peut
donc que deplacer un axe dans son couloir, jamais en supprimer.
}
\seealso{
\code{\link[=dsr_pathfinder]{dsr_pathfinder()}}, \code{\link[=dsr_conductivite]{dsr_conductivite()}}, \code{\link[=dsr_measure]{dsr_measure()}}.
}

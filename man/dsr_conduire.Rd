% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent.R
\name{dsr_conduire}
\alias{dsr_conduire}
\title{Suivre une route depuis une amorce (agent conducteur)}
\usage{
dsr_conduire(
  sigma,
  amorce,
  reseau = NULL,
  portee = 100,
  fov = 160,
  conductivite_min = 0.6,
  seuil = 0.1,
  tampon = 10,
  avance = 0.8,
  trouee_max = 2.5,
  max_pas = 500
)
}
\arguments{
\item{sigma}{Conductivite (\code{SpatRaster} mono-couche), typiquement la sortie de
\code{\link[=dsr_conductivite]{dsr_conductivite()}} ou une carte de probabilite. \code{NA} admis.}

\item{amorce}{Amorce orientee : un \code{LINESTRING} \code{sf}/\code{sfc}. Son dernier point
est la position de depart, sa direction donne le cap initial.}

\item{reseau}{Reseau deja vectorise (\code{sf}/\code{sfc}), rendu infranchissable :
l'agent s'arrete en le rejoignant. \code{NULL} (defaut) pour aucun.}

\item{portee}{Distance de visee, en metres. Defaut 100.}

\item{fov}{Champ de vision total, en degres. Defaut 160.}

\item{conductivite_min}{Conductivite en deca de laquelle une direction n'est
plus consideree comme roulable. Fixe le cout maximal admissible
(\code{portee / conductivite_min}). Defaut 0.6.}

\item{seuil}{Plancher de conductivite applique a la fenetre de travail.
Defaut 0.1.}

\item{tampon}{Demi-largeur (m) de neutralisation du reseau et de la trace
deja parcourue. Defaut 10.}

\item{avance}{Fraction de la portee effectivement parcourue a chaque pas.
Defaut 0.8 : avancer de la portee entiere ferait manquer les
embranchements situes juste avant le point vise.}

\item{trouee_max}{Longueur maximale de trouee toleree, en multiples de la
portee. Defaut 2.5.}

\item{max_pas}{Nombre maximal de pas. Defaut 500.}
}
\value{
Une liste : \code{route} (\code{sfc} \code{LINESTRING}, l'amorce comprise),
\code{amorces} (\code{sfc} des embranchements rencontres, ou \code{NULL}), \code{n_pas},
et \code{arret} (motif d'arret).
}
\description{
Vectorise une route en la \strong{parcourant} : depuis une amorce orientee, l'agent
avance par pas, regarde en eventail devant lui, et part vers la direction la
moins couteuse. Reimplementation terra/sf de l'algorithme de vecnet
(Roussel \emph{et al.} 2023) adossee au noyau Rust du paquet.
}
\details{
\strong{Ce que cette fonction fait et que \code{\link[=dsr_pathfinder]{dsr_pathfinder()}} ne fait pas.} Le
pathfinder relie deux points connus. L'agent ne sait pas ou il va : il suit
la conductivite. Il en decoule deux proprietes que le squelette et le
pathfinder n'ont pas :
\itemize{
\item \strong{robustesse aux trouees} -- une coupure de conductivite est franchie si
la route reprend derriere, parce que le cout admissible est module par la
\emph{profondeur} du creux dans le profil angulaire et non par sa seule valeur ;
\item \strong{decouverte des embranchements} -- les directions ecartees a chaque pas
sont autant d'amorces, rendues dans \code{amorces}, a repasser a la fonction
pour explorer le reseau de proche en proche.
}

\strong{Arret.} L'agent s'arrete quand il rejoint \code{reseau}, quand aucune direction
n'est admissible, quand il sort de l'emprise, quand il a roule trop longtemps
au-dessus du cout maximal (\code{trouee_max} fois la portee), ou apres \code{max_pas}
pas.
}
\examples{
# Carte synthetique : une route rectiligne de conductivite 1 sur fond a 0.1.
r <- terra::rast(nrows = 100, ncols = 100, xmin = 0, xmax = 200,
  ymin = 0, ymax = 200, crs = "EPSG:2154")
terra::values(r) <- 0.1
xy <- terra::xyFromCell(r, seq_len(terra::ncell(r)))
r[abs(xy[, 2] - 100) < 4] <- 1
amorce <- sf::st_sfc(sf::st_linestring(cbind(c(10, 25), c(100, 100))),
  crs = "EPSG:2154")
ans <- dsr_conduire(r, amorce, portee = 40)
ans$arret
}
\seealso{
\code{\link[=dsr_pathfinder]{dsr_pathfinder()}}, \code{\link[=dsr_vectoriser]{dsr_vectoriser()}}, \code{\link[=dsr_reseau]{dsr_reseau()}}.
}

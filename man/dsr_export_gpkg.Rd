% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{dsr_export_gpkg}
\alias{dsr_export_gpkg}
\title{Exporter les couches d'un massif en GeoPackage}
\usage{
dsr_export_gpkg(couches, fichier, styles = TRUE, overwrite = TRUE)
}
\arguments{
\item{couches}{Liste \strong{nommee} d'objets \code{sf} (p. ex. \code{list(trace = ..., stations = ..., etat = ..., reseau = ...)}).}

\item{fichier}{Chemin du GeoPackage de sortie.}

\item{styles}{Ecrire les fichiers \code{.qml} pour les couches stylables. Defaut
\code{TRUE}.}

\item{overwrite}{Ecraser un GeoPackage existant. Defaut \code{TRUE}.}
}
\value{
Le chemin du GeoPackage, invisiblement.
}
\description{
Ecrit un jeu de couches vectorielles \code{sf} dans un unique GeoPackage, une
couche par element nomme (BRIEF section 3). Ecrit optionnellement les styles
QGIS accompagnant les couches reconnues (\code{etat}, \code{aptitude}).
}
\examples{
\donttest{
tr <- sf::st_sf(id = 1, geometry = sf::st_sfc(
  sf::st_linestring(cbind(c(0, 10), c(0, 0))), crs = 2154))
f <- tempfile(fileext = ".gpkg")
dsr_export_gpkg(list(trace = tr), f)
sf::st_layers(f)$name
}
}
\seealso{
\code{\link[=dsr_qml_categorise]{dsr_qml_categorise()}}, \code{\link[=dsr_rapport]{dsr_rapport()}}.
}

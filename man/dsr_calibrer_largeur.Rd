% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measure.R
\name{dsr_calibrer_largeur}
\alias{dsr_calibrer_largeur}
\title{Calibrer la mesure de largeur sur une reference terrain}
\usage{
dsr_calibrer_largeur(
  traces,
  mnt,
  reference,
  champ_largeur,
  grille = NULL,
  long_min = 30,
  confiance = NULL,
  seuils_confiance = c(0, 2, 5, Inf),
  ...
)
}
\arguments{
\item{traces}{\code{sf} des troncons a mesurer.}

\item{mnt}{Le MNT (\code{SpatRaster}).}

\item{reference}{\code{sf} portant une largeur de reference \strong{mesuree
independamment} (voir Details) — pas la sortie d'un autre algorithme.}

\item{champ_largeur}{Nom de la colonne de \code{reference} portant la largeur (m).}

\item{grille}{\code{data.frame} des combinaisons a essayer ; une colonne par
argument de \code{\link[=dsr_measure]{dsr_measure()}} a faire varier. \code{NULL} (defaut) balaie
\code{methode_largeur} x \code{tol_planeite}.}

\item{long_min}{Longueur minimale (m) d'un troncon mesure. Defaut 30.}

\item{confiance}{\code{SpatRaster} de confiance pour la stratification ; \code{NULL}
pour ne pas stratifier.}

\item{seuils_confiance}{Bornes de stratification de \code{confiance}. Defaut
\code{c(0, 2, 5, Inf)} (points sol par m2).}

\item{...}{Arguments communs transmis a \code{\link[=dsr_measure]{dsr_measure()}}.}
}
\value{
Un \code{data.frame} : les colonnes de \code{grille}, puis \code{n} (stations
appariees), \code{biais} (mesure - reference, m), \code{mae}, \code{rmse}, \code{med_dsr},
\code{med_ref}. Trie par \code{mae} croissante. Si \code{confiance} est fourni, une ligne
par combinaison \strong{et} par classe de confiance, avec la colonne \code{strate}.
}
\description{
Balaie une grille de parametres de \code{\link[=dsr_measure]{dsr_measure()}} et confronte la largeur
mesuree a une largeur de reference (releves du gestionnaire, GNSS,
photo-interpretation), station par station. Renvoie le tableau des ecarts,
trie du meilleur au pire, pour arbitrer sur des chiffres plutot que sur une
impression.
}
\details{
\strong{Ce qui peut servir de reference, et ce qui ne le peut pas.} Le calibrage
retient le reglage qui \strong{minimise l'ecart} : pointer cette fonction vers la
sortie d'un autre algorithme ne mesure donc pas un biais, il le \emph{reproduit} —
on selectionne les parametres qui imitent le mieux l'autre methode, defauts
compris. Une reference doit etre \strong{independante de toute mesure automatique}
: releve au decametre, GNSS, ou photo-interpretation sur ortho THR.

Deux sources tentantes qui n'en sont pas :
\itemize{
\item la \strong{largeur declarative d'une base cartographique} (\code{LARGEUR_DE_CHAUSSEE}
de la BD TOPO, par exemple) est souvent defautee par classe et vide sur les
chemins et sentiers — elle ne soutient pas un calibrage au decimetre. Elle
reste utile comme controle \strong{ordinal} : la largeur mesuree doit se ranger
dans l'ordre des classes ;
\item la \strong{sortie d'un traitement anterieur} (desserte corrigee par ALSroads ou
equivalent) est disqualifiee par construction.
}

\strong{Le calibrage ne peut pas commencer par le seuil.} Avec
\code{methode_largeur = "gradient"}, le biais depend du pas transversal et du
lissage autant que de \code{seuil_devers} : la valeur trouvee ne vaudrait que pour
un triplet de parametres et un niveau de bruit. C'est pourquoi le defaut est
\code{"planeite"}, dont le biais est stable (voir \code{\link[=dsr_measure]{dsr_measure()}}) — un seuil cale
sur un massif a alors une chance d'etre transferable.

\strong{Separer le biais de mesure de l'ecart de definition.} Un biais residuel
constant, une fois la methode stabilisee, ne signale pas une erreur de mesure
mais un desaccord sur ce qu'on mesure : la largeur roulable retient la bande
de faible devers, tandis qu'une « largeur carrossable » de gestionnaire inclut
souvent les accotements. C'est une question a trancher avec le gestionnaire,
pas un parametre a tordre. Regarder \code{biais} et \code{mae} ensemble : un biais
constant avec une MAE faible est un decalage de definition ; une MAE forte
avec un biais faible est du bruit de mesure.

\strong{Stratifier.} La qualite du MNT commande tout (BRIEF, risque n.3) : passer
\code{confiance} (typiquement \code{densite_sol} de \code{\link[=dsr_layers_pc]{dsr_layers_pc()}}) fait sortir les
ecarts par classe de confiance. Un biais qui se creuse quand la densite de
points sol s'effondre n'appelle pas un autre seuil, il appelle une reserve sur
le domaine de validite.
}
\seealso{
\code{\link[=dsr_measure]{dsr_measure()}}, \code{\link[=dsr_layers_pc]{dsr_layers_pc()}}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emprise.R
\name{dsr_emprise_certu}
\alias{dsr_emprise_certu}
\title{Emprise routiere normative (methode Certu, fiche 1.7)}
\usage{
dsr_emprise_certu(
  troncons,
  schema = c("auto", "v2", "v3"),
  champs = NULL,
  nature_map = NULL,
  emprise = FALSE
)
}
\arguments{
\item{troncons}{\code{sf} des troncons de route (BD TOPO).}

\item{schema}{\code{"auto"} (defaut), \code{"v2"} ou \code{"v3"}.}

\item{champs}{Liste nommee pour forcer les noms de colonnes (\code{cl_admin},
\code{nature}, \code{franchissement}, \code{nb_voies}, \code{pos_sol}). Elle \strong{complete} la
detection automatique : les champs non cites restent detectes. \code{NULL}
(defaut) pour s'en remettre entierement a la detection.}

\item{nature_map}{Vecteur nomme de correspondance des valeurs de nature vers
celles de la fiche ; \code{NULL} pour le defaut du schema detecte.}

\item{emprise}{\code{TRUE} pour renvoyer en plus les polygones d'emprise
(tampon de \code{largeur_buffer} autour de l'axe). Defaut \code{FALSE}.}
}
\value{
Le \code{sf} d'entree, augmente de \code{LARGEUR_CHAUSSEE_CERTU},
\code{LARGEUR_EMPRISE_CERTU} (= 2 x tampon), \code{BANDE_ARRET_CERTU},
\code{BERME_CERTU}. Les troncons non apparies recoivent \code{NA}. L'attribut
\code{"certu"} porte le schema retenu, les champs utilises et les combinaisons
non appariees. Avec \code{emprise = TRUE}, une liste \code{troncons} / \code{emprise}.
}
\description{
Estime la largeur de chaussee et l'emprise d'un troncon a partir des seuls
attributs de la BD TOPO, par les largeurs standard de la fiche Certu 1.7
(2013). C'est un \strong{calcul normatif}, pas une mesure : il dit ce que la
reglementation suppose, pas ce que le terrain porte.
}
\details{
\strong{Ce n'est pas une reference pour calibrer une mesure.} Pour toute la
desserte forestiere  \code{Chemin}, \verb{Route empierree}, \code{Sentier}  la fiche rend
une \strong{constante de 2 m}, identique quelle que soit la route. Caler
\code{\link[=dsr_measure]{dsr_measure()}} dessus forcerait la mesure a 2 m partout, c'est-a-dire
detruirait le signal que le paquet existe pour produire. La fiche pose
elle-meme ses limites : elle a ecarte le champ de largeur de la BD TOPO
( pas renseigne de facon homogene ), ses valeurs  ne delimitent pas avec
une precision decimetrique , et la methode  surestime  sur la voirie
locale.

Son interet est ailleurs :
\itemize{
\item \strong{le vocabulaire}  la fiche donne la decomposition normative du profil en
travers, et permet de situer ce que mesure \code{LARGEUR_ROULABLE} : la
\strong{chaussee}, comparable a \code{LARGEUR_CHAUSSEE_CERTU}, et non l'emprise, qui
ajoute bande derasee et berme ;
\item \strong{l'ecart a la norme}  c'est la lecture utile. dessertR mesure ce que la
fiche ne peut que supposer :  cette piste fait 3,2 m la ou la norme en
suppose 2  informe le gestionnaire, dans ce sens-la et pas l'inverse.
}

\strong{Schemas.} La fiche est ecrite pour la BD TOPO v2 (champs \code{cl_admin},
\code{nature}, \code{franchisst}, \code{nb_voies}). En v3 les noms different et le
franchissement se deduit de \code{pos_sol} (negatif : tunnel, positif : pont).
Les correspondances de valeurs sont \strong{deduites et non officielles} : la
sortie liste les combinaisons non appariees plutot que de leur affecter un
defaut silencieux.
}
\references{
Certu, CETE Nord-Picardie et Mediterranee (2013). \emph{Mesure de la
consommation d'espace : methodes et indicateurs}, fiche 1.7,  Surfaces
occupees par les infrastructures routieres .
}
\seealso{
\code{\link[=dsr_measure]{dsr_measure()}}, \code{\link[=dsr_calibrer_largeur]{dsr_calibrer_largeur()}}.
}

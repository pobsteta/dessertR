% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers_pc.R
\name{dsr_layers_pc}
\alias{dsr_layers_pc}
\title{Canaux de surface et de qualite du nuage de points}
\usage{
dsr_layers_pc(
  dalle,
  res = DSR_RES_MULTIECHELLE,
  grille = NULL,
  emprise = NULL,
  masque = NULL,
  sousetage = c(0.3, 3),
  classe_sol = 2L,
  classe_couvert = 5L,
  classe_pont = 17L,
  classes_exclusion = c(6L, 9L, 64L, 66L),
  seuil_couvert = 0.95
)
}
\arguments{
\item{dalle}{Chemin d'un fichier LAZ/LAS/COPC, ou une ligne de
\code{\link[=dsr_catalog]{dsr_catalog()}} (colonne \code{laz}).}

\item{res}{Resolution des rasters en metres. Defaut 1 (aligne sur la grille de
reference ; 2 suffit pour les seules metriques de couvert).}

\item{grille}{Grille de reference (\code{\link[=dsr_grille_reference]{dsr_grille_reference()}}) pour aligner les
sorties ; \code{NULL} conserve la grille native de \code{lasR}.}

\item{emprise}{Restreint la \strong{lecture} a un rectangle : \code{NULL} (dalle
entiere), un vecteur \code{c(xmin, ymin, xmax, ymax)}, ou un objet \code{sf}/\code{sfc}/
\code{SpatVector} (son emprise est utilisee).}

\item{masque}{Objet \code{sf}/\code{sfc}/\code{SpatVector} pour \strong{decouper} finement les
sorties (corridor) ; \code{NULL} pour ne pas masquer.}

\item{sousetage}{Bornes de hauteur du sous-etage, en metres. Defaut
\code{c(0.3, 3)}.}

\item{classe_sol, classe_couvert, classe_pont}{Codes ASPRS du sol (2), de la
vegetation haute (5) et du tablier de pont (17).}

\item{classes_exclusion}{Codes ASPRS a neutraliser. Defaut \code{c(6, 9, 64, 66)}
(batiment, eau, sursol perenne, points virtuels).}

\item{seuil_couvert}{Percentile de hauteur pour \code{h_couvert}. Defaut 0.95.}
}
\value{
Un \code{SpatRaster} multi-bandes aligne (si \code{grille} fourni) portant les
canaux ci-dessus. Noms compatibles avec la suite du traitement (\code{sigma_surf},
etat, confiance de \code{\link[=dsr_conductivite]{dsr_conductivite()}}).
}
\description{
Rasterise, via \code{lasR}, les metriques du nuage classe qui portent l'etat de
surface d'une desserte et la confiance du MNT (BRIEF section 3.3), sur une
grille alignee sur le MNT :
}
\details{
\describe{
\item{\code{densite_sol}}{points sol (classe 2) au m2 -- \strong{couche de confiance}
du MNT, pas de detection.}
\item{\code{taux_penetration}}{points sol / points totaux -- ouverture de la
canopee au-dessus de l'emprise.}
\item{\code{densite_sousetage}}{echos entre \code{sousetage} m au-dessus du sol,
rapportes au total -- \strong{recolonisation de l'emprise, le signal d'abandon},
inaccessible au MNH.}
\item{\code{h_couvert}}{percentile haut de la vegetation haute (hauteur au sol).}
\item{\code{masque_exclusion}}{1 la ou une classe neutralisee (eau, points
virtuels, sursol perenne, batiment) est presente.}
\item{\code{masque_pont}}{1 la ou la classe tablier de pont est presente --
corridors franchissables.}
}

Les hauteurs au sol sont obtenues par \code{lasR::normalize()} (TIN des points sol).
Les points virtuels sont exclus du calcul de \code{densite_sol}. Le regime corridor
(lecture restreinte a l'emprise) rend le traitement d'un massif realiste :
passer \code{emprise} limite la lecture a un rectangle ; passer en plus \code{masque}
decoupe finement les sorties au corridor.
}
\seealso{
\code{\link[=dsr_conductivite]{dsr_conductivite()}} (consomme \code{densite_sol} comme confiance),
\code{\link[=dsr_grille_reference]{dsr_grille_reference()}}.
}

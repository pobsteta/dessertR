% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers_dtm.R
\name{dsr_layers_dtm}
\alias{dsr_layers_dtm}
\title{Assembler le canal geomorphologique complet}
\usage{
dsr_layers_dtm(
  mnt,
  grille = NULL,
  res = DSR_RES_MULTIECHELLE,
  rayons_openness = c(2, 5, 10),
  echelles_vessel = c(1, 2, 4),
  c_vessel = NULL,
  fenetres_slrm = c(5, 15),
  fenetre_rugosite = 5
)
}
\arguments{
\item{mnt}{Le MNT (\code{SpatRaster} ou chemin), typiquement a 50 cm.}

\item{grille}{Grille de reference (\code{\link[=dsr_grille_reference]{dsr_grille_reference()}}) ; \code{NULL} (defaut)
-> derivee du MNT a \code{res}.}

\item{res}{Resolution de la grille si \code{grille} est \code{NULL}. Defaut 1.}

\item{rayons_openness}{Rayons d'openness negative multi-echelle, en metres.
Defaut \code{c(2, 5, 10)}.}

\item{echelles_vessel}{Echelles de la vesselness, en metres. Defaut
\code{c(1, 2, 4)}.}

\item{c_vessel}{Sensibilite de la vesselness, \strong{une valeur par echelle}
(aligne sur \code{echelles_vessel}) ou un scalaire. \code{NULL} (defaut) conserve le
comportement historique : \code{c} derive de l'image, donc \strong{une sortie qui
depend de l'etendue analysee}. Fixer ce vecteur -- via \code{\link[=dsr_c_vessel]{dsr_c_vessel()}}
sur l'emprise de reference du chantier -- rend la pile comparable d'une
fenetre a l'autre. Voir \code{\link[=dsr_vesselness]{dsr_vesselness()}}.}

\item{fenetres_slrm}{Fenetres du SLRM, en metres. Defaut \code{c(5, 15)}.}

\item{fenetre_rugosite}{Fenetre de la rugosite, en metres. Defaut 5.}
}
\value{
Un \code{SpatRaster} multi-bandes aligne sur la grille : \code{pente},
\code{rugosite}, \verb{slrm_*}, \verb{openness_neg_*}, \code{openness_pos}, \code{svf}, \code{vesselness},
\code{theta}.
}
\description{
Construit toute la pile de couches geomorphologiques (BRIEF section 3.2) sur
la \strong{grille de reference} (1 m par defaut), a partir du seul MNT. C'est
l'entree du calcul de \code{sigma_geo} (\code{\link[=dsr_conductivite]{dsr_conductivite()}}).
}
\details{
Le MNT est d'abord reechantillonne sur la grille de reference ; toutes les
couches sont donc nativement alignees, sans reechantillonnage a posteriori.
}
\seealso{
\code{\link[=dsr_micro_relief]{dsr_micro_relief()}}, \code{\link[=dsr_conductivite]{dsr_conductivite()}}.
}

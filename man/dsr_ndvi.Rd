% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canopee.R
\name{dsr_ndvi}
\alias{dsr_ndvi}
\title{Indice de vegetation normalise (NDVI) depuis une ortho infrarouge}
\usage{
dsr_ndvi(irc, bandes = c(pir = 1, rouge = 2))
}
\arguments{
\item{irc}{\code{SpatRaster} multi-bandes de l'ortho IRC.}

\item{bandes}{Indices ou noms des bandes proche infrarouge et rouge. Defaut
\code{c(pir = 1, rouge = 2)}, l'ordre de la BD ORTHO IRC.}
}
\value{
Un \code{SpatRaster} a une bande nommee \code{ndvi}, valeurs dans \verb{[-1, 1]}.
}
\description{
Calcule le NDVI \code{(PIR - Rouge) / (PIR + Rouge)} a partir d'une ortho
infrarouge couleur -- la BD ORTHO IRC de l'IGN, dont les bandes sont dans
l'ordre PIR, Rouge, Vert. Contrairement a un modele de hauteur de canopee
predit, le NDVI est calcule sur les pixels \strong{natifs} de l'ortho (20 cm) :
c'est la seule grandeur optique a l'echelle d'une chaussee forestiere.
}
\details{
Le rapport est sans dimension : aucune normalisation radiometrique n'est
requise, des comptes numeriques 8 bits conviennent. Les pixels ou
\code{PIR + Rouge} s'annule sont mis a \code{NA}.

Limites, a garder en tete avant d'en tirer une largeur : sous couvert ferme
la chaussee est a l'ombre et son NDVI remonte ; une piste enherbee ne se
distingue pas du sous-bois ; le millesime de l'ortho n'est pas celui du
lidar.
}
\examples{
irc <- terra::rast(xmin = 0, xmax = 10, ymin = 0, ymax = 10,
  resolution = 1, nlyrs = 3, crs = "EPSG:2154")
terra::values(irc) <- cbind(rep(200, 100), rep(50, 100), rep(60, 100))
terra::global(dsr_ndvi(irc), "mean")
}
\seealso{
\code{\link[=dsr_largeur_ndvi]{dsr_largeur_ndvi()}}, \code{\link[=dsr_canaux_externes]{dsr_canaux_externes()}} pour verser le NDVI
dans la pile de canaux.
}

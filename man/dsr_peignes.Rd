% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classer.R
\name{dsr_peignes}
\alias{dsr_peignes}
\title{Regrouper les traces en peignes de paralleles}
\usage{
dsr_peignes(
  traces,
  tol_angle = 15,
  espacement_min = 4,
  espacement_max = 40,
  n_min = 3,
  regularite = 0.5
)
}
\arguments{
\item{traces}{\code{sf} de \code{LINESTRING}.}

\item{tol_angle}{Ecart de direction admis dans un groupe, en degres. Defaut
15.}

\item{espacement_min, espacement_max}{Bornes de recherche de l'espacement
entre dents, en metres. Defauts 4 et 40.}

\item{n_min}{Nombre minimal de dents. Defaut 3.}

\item{regularite}{Ecart relatif maximal a l'espacement median. Defaut 0.5.}
}
\value{
Le \code{sf} d'entree, augmente de \code{PEIGNE} (identifiant, \code{NA} hors
peigne), \code{PEIGNE_N} (nombre de dents) et \code{PEIGNE_ESPACEMENT} (m, median).
}
\description{
Repere les faisceaux de lineaires \strong{paralleles et regulierement espaces} --
la signature d'un cloisonnement d'exploitation, qui n'existe jamais seul.
}
\details{
\strong{Pourquoi une structure plutot qu'une largeur.} Un cloisonnement se
reconnait a son peigne, pas a sa section : les largeurs de cloisonnement
varient avec le peuplement et le materiel, et la largeur que mesure
\code{\link[=dsr_measure]{dsr_measure()}} est souvent une plateforme (voir \code{BORDS_CHAUSSEE}). La
periodicite, elle, est \strong{estimee sur la donnee} : aucune valeur d'espacement
n'est posee a priori, seules les bornes de recherche le sont.

La direction d'une trace est prise entre ses extremites, modulo 180 degres.
Les traces de direction voisine sont regroupees, projetees sur la normale a
la direction moyenne du groupe, puis triees : un peigne est une suite d'au
moins \code{n_min} traces dont les ecarts successifs tiennent dans
\verb{[espacement_min, espacement_max]} et ne s'ecartent pas de plus de
\code{regularite} de leur mediane.

\strong{Limite assumee} : deux traces colineaires (bout a bout) se projettent au
meme endroit ; \code{espacement_min} les ecarte du peigne plutot que de les
compter comme deux dents.
}
\examples{
g <- lapply(seq(0, 60, by = 20), function(y)
  sf::st_linestring(cbind(c(0, 100), c(y, y))))
tr <- sf::st_sf(geometry = sf::st_sfc(g, crs = 2154))
dsr_peignes(tr)$PEIGNE
}
\seealso{
\code{\link[=dsr_classer]{dsr_classer()}}, \code{\link[=dsr_dedupe_paralleles]{dsr_dedupe_paralleles()}}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conductivity.R
\name{dsr_appartenance}
\alias{dsr_appartenance}
\title{Fonction d'appartenance floue}
\usage{
dsr_appartenance(
  x,
  type = c("croissante", "decroissante", "cloche"),
  a = NULL,
  b = NULL,
  marge = NULL
)
}
\arguments{
\item{x}{Un \code{SpatRaster} mono-couche, ou un vecteur numerique.}

\item{type}{\code{"croissante"}, \code{"decroissante"} ou \code{"cloche"}.}

\item{a, b}{Bornes de la rampe (\code{a} = debut, \code{b} = fin). Pour \code{"cloche"}, \code{a}
et \code{b} delimitent le plateau et \code{marge} sa retombee. \code{NULL} -> quantiles
(\code{croissante}/\code{decroissante} : 0.5 et 0.95 ; \code{cloche} : 0.25 et 0.75).}

\item{marge}{Largeur des flancs de la cloche (unites de \code{x}). \code{NULL} ->
\code{(b - a) / 2}.}
}
\value{
Un objet de meme type que \code{x} (raster ou vecteur), valeurs dans
\verb{[0, 1]}.
}
\description{
Transforme un canal continu en un degre d'appartenance dans \verb{[0, 1]} (BRIEF
section 3.4). Trois formes : rampe croissante, rampe decroissante, ou cloche
(plateau trapezoidal). Quand les bornes ne sont pas fournies, elles sont
derivees des quantiles des donnees — pratique pour une premiere conductivite
\emph{inspectable} avant calibration, a figer ensuite sur un jeu de validation.
}
\examples{
v <- c(0, 25, 50, 75, 100)
dsr_appartenance(v, "croissante", a = 20, b = 80)
}
\seealso{
\code{\link[=dsr_conductivite]{dsr_conductivite()}}.
}

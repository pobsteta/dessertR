% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state.R
\name{dsr_etat}
\alias{dsr_etat}
\title{Etat de la desserte par croisement des deux conductivites}
\usage{
dsr_etat(sigma_geo, sigma_surf, seuil_geo = 0.5, seuil_surf = 0.5)
}
\arguments{
\item{sigma_geo, sigma_surf}{\code{SpatRaster} mono-couche alignes, dans \verb{[0, 1]}
(sorties de \code{\link[=dsr_conductivite]{dsr_conductivite()}} et \code{\link[=dsr_sigma_surf]{dsr_sigma_surf()}}).}

\item{seuil_geo, seuil_surf}{Seuils de bascule fort/faible. Defaut 0.5.}
}
\value{
Un \code{SpatRaster} categoriel \code{etat} (facteur a 4 niveaux, voir
description), aligne sur les entrees.
}
\description{
Classe chaque cellule en quatre etats selon le croisement de \code{sigma_geo}
(empreinte dans le terrain) et \code{sigma_surf} (emprise encore degagee), par
seuillage (BRIEF section 3.4) :
}
\details{
\describe{
\item{\code{en_service}}{\code{sigma_geo} fort et \code{sigma_surf} fort.}
\item{\code{abandonnee}}{\code{sigma_geo} fort et \code{sigma_surf} faible -- \strong{le signal
recherche} : route recolonisee.}
\item{\code{trouee_sans_route}}{\code{sigma_geo} faible et \code{sigma_surf} fort --
faux positif geomorphologique (coupe rase, ligne electrique, layon).}
\item{\code{hors_route}}{les deux faibles.}
}

L'etat n'est reellement interpretable que \strong{le long d'un trace retenu} par le
pathfinder ; en raster plein il sert d'inspection et de diagnostic.
}
\examples{
\donttest{
sg <- terra::rast(nrows = 4, ncols = 4, xmin = 0, xmax = 4, ymin = 0, ymax = 4,
  crs = "EPSG:2154")
ss <- terra::rast(sg)
terra::values(sg) <- c(rep(0.9, 8), rep(0.1, 8))
terra::values(ss) <- rep(c(0.9, 0.1), 8)
e <- dsr_etat(sg, ss)
terra::levels(e)
}
}
\seealso{
\code{\link[=dsr_conductivite]{dsr_conductivite()}}, \code{\link[=dsr_sigma_surf]{dsr_sigma_surf()}}, \code{\link[=dsr_divergence]{dsr_divergence()}}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers_dtm.R
\name{dsr_c_vessel}
\alias{dsr_c_vessel}
\title{Ancrer l'echelle de la vesselness sur une emprise de reference}
\usage{
dsr_c_vessel(mnt, echelles_m = c(1, 2, 4))
}
\arguments{
\item{mnt}{Le MNT de reference (\code{SpatRaster} ou chemin). Prendre l'emprise la
plus large du chantier : c'est elle qui fixe le bareme.}

\item{echelles_m}{Echelles, en metres. Doit valoir ce qui sera passe ensuite a
\code{\link[=dsr_vesselness]{dsr_vesselness()}}. Defaut \code{c(1, 2, 4)}.}
}
\value{
Un vecteur numerique nomme (\code{c_1}, \code{c_2}, ...), une valeur par
echelle.
}
\description{
Calcule, echelle par echelle, le \code{c} que \code{\link[=dsr_vesselness]{dsr_vesselness()}} deriverait de
\code{mnt} -- la moitie du maximum de la norme de Frobenius du Hessien. Le
resultat se repasse tel quel a \code{\link[=dsr_vesselness]{dsr_vesselness()}} ou \code{\link[=dsr_layers_dtm]{dsr_layers_dtm()}} pour
que la vesselness cesse de dependre de l'etendue analysee.
}
\details{
\strong{Le probleme.} Sans \code{c} fixe, la vesselness est relative a l'emprise
fournie : le meme terrain, analyse seul ou au sein d'un bloc plus vaste, ne
rend pas la meme valeur. Le defaut agit \strong{en amont} des fonctions
d'appartenance, donc aucun reglage de bornes dans
\code{\link[=dsr_specs_geomorpho]{dsr_specs_geomorpho()}} ne le rattrape, et il touche aussi le
\code{seuil_vessel} de \code{\link[=dsr_indice_detection]{dsr_indice_detection()}}, ou une vesselness rescalee est
comparee a un seuil absolu.

\strong{L'usage.} Calculer une fois sur l'emprise de reference du chantier -- le
massif entier, pas la fenetre du jour -- puis passer le vecteur obtenu a tous
les traitements ulterieurs. Deux fenetres traitees avec le meme \code{c} sont
alors comparables, y compris en regime \code{corridor}.
}
\examples{
mnt <- terra::rast(nrows = 40, ncols = 40, xmin = 0, xmax = 40, ymin = 0,
  ymax = 40, crs = "EPSG:2154")
terra::values(mnt) <- runif(1600)
dsr_c_vessel(mnt, echelles_m = c(1, 2))
}
\seealso{
\code{\link[=dsr_vesselness]{dsr_vesselness()}}, \code{\link[=dsr_layers_dtm]{dsr_layers_dtm()}}.
}

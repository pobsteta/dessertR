% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emprise.R
\name{dsr_ecart_norme}
\alias{dsr_ecart_norme}
\title{Ecart a la norme Certu, troncon par troncon}
\usage{
dsr_ecart_norme(
  stations,
  certu,
  id = "troncon",
  champ_mesure = "LARGEUR_ROULABLE",
  champ_certu = "LARGEUR_CHAUSSEE_CERTU"
)
}
\arguments{
\item{stations}{\code{sf}/\code{data.frame} des stations (\code{\link[=dsr_measure]{dsr_measure()}}), portant
\code{champ_mesure}, la colonne \code{id} et, si possible, \code{BORDS_CHAUSSEE}.}

\item{certu}{Sortie de \code{\link[=dsr_emprise_certu]{dsr_emprise_certu()}} (ou tout objet portant
\code{champ_certu}).}

\item{id}{Nom de la colonne identifiant le troncon dans \code{stations}. Defaut
\code{"troncon"}.}

\item{champ_mesure, champ_certu}{Colonnes comparees. Defauts
\code{"LARGEUR_ROULABLE"} et \code{"LARGEUR_CHAUSSEE_CERTU"}.}
}
\value{
Un \code{data.frame}, une ligne par troncon mesure : la colonne \code{id},
\code{N_STATIONS}, \code{LARGEUR_MED} (mediane mesuree), \code{LARGEUR_NORME},
\code{ECART_NORME} (mesure - norme, m), \code{ECART_REL} (rapporte a la norme) et
\code{BORDS_RESOLUS} (part de stations ou la chaussee est resolue, \code{NA} si
\code{BORDS_CHAUSSEE} est absent). Les troncons que la fiche n'apparie pas
gardent \code{LARGEUR_NORME = NA} et un ecart \code{NA} : ils sont conserves, pas
silencieusement retires.
}
\description{
Confronte la largeur MESUREE (\code{\link[=dsr_measure]{dsr_measure()}}) a la largeur NORMATIVE de la
fiche Certu (\code{\link[=dsr_emprise_certu]{dsr_emprise_certu()}}). Le sens de lecture est fixe : la mesure
informe sur ce que la norme ne fait que supposer -- « cette piste fait 3,2 m
la ou la fiche en suppose 2 ». L'inverse, ramener la mesure vers la norme,
detruirait le signal que le paquet produit (voir \code{\link[=dsr_emprise_certu]{dsr_emprise_certu()}}).
}
\details{
\strong{Comparer ce qui est comparable.} \code{LARGEUR_ROULABLE} vise la chaussee, et
\code{LARGEUR_CHAUSSEE_CERTU} est une largeur de chaussee : les deux se
correspondent. Mais quand la rupture chaussee/accotement n'est pas resolue,
\code{\link[=dsr_measure]{dsr_measure()}} retombe sur la \strong{plateforme} et le signale par
\code{BORDS_CHAUSSEE}. La colonne \code{BORDS_RESOLUS} reporte cette part au niveau du
troncon : proche de 0, l'ecart compare une plateforme a une largeur de
chaussee, et se lit comme un majorant.

\strong{Appariement.} \code{\link[=dsr_measure]{dsr_measure()}} ne nomme pas ses troncons ; l'usage etabli
est d'ajouter soi-meme une colonne (\code{troncon}) portant l'indice de ligne du
reseau mesure. Si \code{certu} porte la meme colonne, l'appariement se fait
dessus ; sinon il se fait par \strong{indice de ligne}, ce qui suppose que \code{certu}
est le meme reseau, dans le meme ordre.
}
\examples{
stations <- data.frame(troncon = c(1, 1, 2, 2),
  LARGEUR_ROULABLE = c(3.0, 3.4, 2.1, 2.3), BORDS_CHAUSSEE = c(2, 2, 0, 0))
certu <- data.frame(LARGEUR_CHAUSSEE_CERTU = c(2, 2))
dsr_ecart_norme(stations, certu)
}
\seealso{
\code{\link[=dsr_emprise_certu]{dsr_emprise_certu()}}, \code{\link[=dsr_measure]{dsr_measure()}}, \code{\link[=dsr_rapport]{dsr_rapport()}}.
}

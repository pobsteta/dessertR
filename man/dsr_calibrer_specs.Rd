% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conductivity.R
\name{dsr_calibrer_specs}
\alias{dsr_calibrer_specs}
\title{Calibrer les regles de conductivite sur un reseau de reference}
\usage{
dsr_calibrer_specs(
  couches,
  reference,
  pres = 3,
  absent = 20,
  auc_min = 0.55,
  poids_max = 3,
  n = 2500,
  exclure = "theta",
  bornes = TRUE
)
}
\arguments{
\item{couches}{Un \code{SpatRaster} multi-bandes (\code{\link[=dsr_layers_dtm]{dsr_layers_dtm()}}), ou une
\strong{liste} de piles -- une par massif.}

\item{reference}{Reseau de reference (\code{sf}/\code{sfc} de lignes), ou une liste de
meme longueur que \code{couches}.}

\item{pres}{Distance (m) en deca de laquelle une cellule compte comme
« sur route ». Defaut 3.}

\item{absent}{Distance (m) au-dela de laquelle une cellule compte comme
« hors route ». Defaut 20. La bande intermediaire est ignoree : c'est le
bord de plateforme, ni route ni environnement.}

\item{auc_min}{AUC minimale pour qu'un canal entre dans les regles. Defaut
0.55. En dessous, le canal n'apporte pas de quoi payer le bruit qu'il
ajoute.}

\item{poids_max}{Poids attribue au canal le plus discriminant ; les autres
sont proportionnels a leur ecart au hasard. Defaut 3.}

\item{n}{Taille des echantillons compares. Defaut 2500.}

\item{exclure}{Canaux ignores. Defaut \code{"theta"}, qui est une orientation et
non une intensite.}

\item{bornes}{Produire aussi les bornes d'appartenance \code{a} et \code{b}, en unites
du canal. Defaut \code{TRUE}. Voir « Bornes absolues » ci-dessous ; \code{FALSE}
rend des regles sans bornes, donc \strong{relatives a l'emprise}.}
}
\value{
Une liste : \code{specs}, directement utilisable comme argument \code{specs} de
\code{\link[=dsr_conductivite]{dsr_conductivite()}}, et \code{diagnostic}, un \code{data.frame} (\code{canal}, \code{auc},
\code{sens}, \code{stable}, \code{retenu}, \code{poids}, \code{a}, \code{b}) trie par pouvoir
discriminant decroissant. Avec plusieurs massifs, \code{auc} est la mediane et
\code{stable} indique si le sens concorde partout.
}
\description{
Mesure, canal par canal, ce qui distingue reellement une route de son
environnement sur \strong{vos} donnees, et en deduit un jeu de regles utilisable
tel quel par \code{\link[=dsr_conductivite]{dsr_conductivite()}}.
}
\details{
\strong{Pourquoi cette fonction existe.} Les regles par defaut
(\code{\link[=dsr_specs_geomorpho]{dsr_specs_geomorpho()}}) reposent sur une intuition physique : une route est
lisse, elle occupe un creux, elle est lineaire. Mesuree sur deux massifs
Lidar HD, l'intuition sur la rugosite est \strong{fausse et inversee} -- une piste
empierree a ornieres est plus rugueuse, a 50 cm, qu'un versant forestier
localement plan. Le canal le plus discriminant des deux jeux (AUC 0,78 et
0,68) etait donc utilise a l'envers, et la conductivite qui en resultait
tombait au niveau du hasard (0,51 et 0,54). Signes corriges, elle remonte a
0,77 et 0,72.

\strong{Ce que la fonction mesure.} Pour chaque canal, l'aire sous la courbe ROC
entre les cellules proches du reseau de reference (\code{pres}) et celles qui en
sont eloignees (\code{absent}). L'AUC est rendue \strong{orientee} : 0,5 signifie
aucun pouvoir discriminant, et \code{sens} dit si le canal marque la route par le
haut ou par le bas. Les canaux multi-echelles sont regroupes par base et
moyennes, comme le fait \code{\link[=dsr_conductivite]{dsr_conductivite()}} -- mesurer autrement
calibrerait autre chose que ce qui sera utilise.

\strong{Plusieurs massifs valent mieux qu'un, et pas seulement pour la precision.}
En passant une liste, un canal n'est retenu que si son sens est \strong{le meme
partout}. Ce n'est pas un raffinement : sur les deux massifs de validation,
la \code{pente} marque les routes par le bas dans l'un et par le haut dans
l'autre. Calibree sur un seul, elle entrait dans les regles ; calibree sur
les deux, elle en est ecartee. Un canal dont le signe depend du relief n'a
rien a faire dans une regle.

\strong{Bornes absolues, et pourquoi elles comptent.} La fonction rend aussi les
bornes \code{a} et \code{b} de chaque rampe, en unites du canal (\code{bornes = TRUE}).
Ce n'est pas un agrement : sans elles, \code{\link[=dsr_appartenance]{dsr_appartenance()}} derive ses bornes
des quantiles de la donnee qu'on lui passe, et \strong{la sortie depend alors de
l'etendue analysee}. Mesure sur le bloc wsfi, une fenetre de 0,25 km2 rend
116 m de desserte detectee analysee seule, et \strong{0 m} analysee au sein de
4 km2 : le \code{seuil} de \code{\link[=dsr_detecter]{dsr_detecter()}} n'est pas une quantite absolue mais un
rang dans la population fournie. Deux sites d'etendues differentes ne sont
pas comparables, et le regime \code{corridor} change le bareme.

La convention est de faire aller la rampe du typique de l'ENVIRONNEMENT
(\code{mu = 0}) au franchement ROUTIER (\code{mu = 1}) :\tabular{lll}{
   sens \tab \code{a} \tab \code{b} \cr
   \code{croissante} \tab q50(absence) \tab q75(presence) \cr
   \code{decroissante} \tab q25(presence) \tab q50(absence) \cr
}


Une borne, contrairement au sens et a l'AUC, est dans l'unite du canal et ne
se transporte pas forcement d'un massif a l'autre -- le taux de penetration
brut varie d'un facteur 7 entre les deux massifs de validation. Avec
plusieurs massifs les bornes sont donc medianes, et \strong{calibrer sur les
massifs qu'on va effectivement traiter reste la bonne pratique}.

Cette correction ne suffit pas a elle seule a rendre une pile independante de
l'emprise : \code{vesselness} est rescalee \strong{en amont} des fonctions
d'appartenance et demande son propre ancrage (\code{\link[=dsr_c_vessel]{dsr_c_vessel()}}).

\strong{Ce que la reference peut et ne peut pas etre.} Sa POSITION doit faire
autorite -- la BD TOPO convient, sa precision planimetrique etant metrique.
Sa largeur, non : elle n'entre pas dans le calcul. Un reseau approximatif
(trace GPS, numerisation sur fond satellite) deplacerait les echantillons
« presence » hors de l'emprise reelle et calibrerait du bruit.
}
\examples{
\donttest{
mnt <- terra::rast(nrows = 60, ncols = 60, xmin = 0, xmax = 60, ymin = 0,
  ymax = 60, crs = "EPSG:2154")
terra::values(mnt) <- runif(3600)
couches <- dsr_layers_dtm(mnt, res = 1)
axe <- sf::st_sfc(sf::st_linestring(cbind(c(5, 55), c(30, 30))), crs = 2154)
cal <- dsr_calibrer_specs(couches, axe)
cal$diagnostic
}
}
\seealso{
\code{\link[=dsr_conductivite]{dsr_conductivite()}}, \code{\link[=dsr_specs_geomorpho]{dsr_specs_geomorpho()}}, \code{\link[=dsr_layers_dtm]{dsr_layers_dtm()}}.
}

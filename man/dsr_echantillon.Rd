% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/learn.R
\name{dsr_echantillon}
\alias{dsr_echantillon}
\title{Echantillon d'apprentissage de la conductivite}
\usage{
dsr_echantillon(
  couches,
  positifs,
  negatifs = NULL,
  buffer_pos = 3,
  buffer_neg = 25,
  emprise = NULL,
  n_max = 20000,
  equilibre = TRUE
)
}
\arguments{
\item{couches}{\code{SpatRaster} multi-bandes des canaux explicatifs — la pile de
\code{\link[=dsr_layers_dtm]{dsr_layers_dtm()}}, celle de \code{\link[=dsr_layers_pc]{dsr_layers_pc()}}, ou leur concatenation.}

\item{positifs}{\code{sf}/\code{sfc} du reseau connu comme reellement present (BD TOPO
verifiee, releves GNSS, desserte du gestionnaire).}

\item{negatifs}{\code{sf}/\code{sfc} des zones connues sans desserte ; \code{NULL} (defaut)
pour prendre tout ce qui est au-dela de \code{buffer_neg} des \code{positifs}.}

\item{buffer_pos}{Demi-largeur (m) du tampon des positifs. Defaut 3.}

\item{buffer_neg}{Distance (m) au-dela de laquelle une cellule est tenue pour
negative. Defaut 25. Ignore si \code{negatifs} est fourni.}

\item{emprise}{\code{sf}/\code{sfc} polygonal restreignant la zone de prelevement ;
\code{NULL} pour toute la grille.}

\item{n_max}{Nombre maximal de cellules prelevees. Defaut 20000.}

\item{equilibre}{\code{TRUE} (defaut) pour tirer autant de positifs que de
negatifs ; \code{FALSE} pour conserver la prevalence reelle.}
}
\value{
Un \code{data.frame} : colonne \code{y} (0/1) puis une colonne par canal.
L'attribut \code{"cellules"} porte les numeros de cellule preleves.
}
\description{
Construit la table d'apprentissage d'une conductivite apprise : une ligne par
cellule retenue, la valeur de chaque canal en colonnes, et l'etiquette \code{y}
(1 = route, 0 = hors route). Les positifs sont les cellules sous le reseau de
reference, les negatifs celles suffisamment eloignees de tout lineaire connu.
}
\details{
\strong{La bande grise est ecartee.} Entre \code{buffer_pos} et \code{buffer_neg} autour du
reseau, aucune cellule n'est prelevee : ce sont les accotements, les fosses et
l'imprecision planimetrique residuelle de la reference. Les y verser
brouillerait les deux classes et le modele apprendrait le flou de la BD TOPO
plutot que la route.

Le tirage est aleatoire : appeler \code{\link[=set.seed]{set.seed()}} en amont pour un echantillon
reproductible.
}
\seealso{
\code{\link[=dsr_apprendre_conductivite]{dsr_apprendre_conductivite()}}, \code{\link[=dsr_conductivite]{dsr_conductivite()}}.
}

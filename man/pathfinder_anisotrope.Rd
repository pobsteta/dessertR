% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{pathfinder_anisotrope}
\alias{pathfinder_anisotrope}
\title{Anisotropic least-cost path with heading state and curvature penalty.}
\usage{
pathfinder_anisotrope(
  sigma,
  theta,
  weight,
  nr,
  nc,
  resolution,
  k,
  lambda,
  mu,
  sigma_min,
  src,
  dst
)
}
\arguments{
\item{sigma}{Geomorphic conductivity, row-major, \code{NA} for impassable.}

\item{theta}{Local line orientation (degrees), row-major; \code{NA} -> isotropic.}

\item{weight}{Anisotropy strength per cell (e.g. vesselness, 0..1), row-major.}

\item{nr, nc}{Raster rows and columns.}

\item{resolution}{Cell size (m).}

\item{k}{Number of discrete headings.}

\item{lambda}{Anisotropy weight (cross-orientation penalty).}

\item{mu}{Curvature weight (heading-change penalty).}

\item{sigma_min}{Conductivity floor (avoids infinite resistance).}

\item{src, dst}{Source and target cell indices (0-based, row-major).}
}
\value{
A list with \code{path} (1-based cell indices, source to target), \code{cost}
(total, \code{NA} if unreachable) and \code{cumcost} (min cost-to-source per cell).
}
\description{
Runs a Dijkstra over a (cell, heading) state space on the geomorphic
conductivity, with a 16-neighbourhood to curb Dijkstra's metrication bias, a
cross-orientation anisotropy penalty (a road has a direction), and a
curvature penalty on heading changes (BRIEF section 3.5). No GIS in the crate:
R passes the flat grids and cell indices, and re-attaches the trace.
}
\keyword{internal}
